package com.loiane.cursojava.lab4752;

import java.util.Scanner;

public class Agenda {

	Scanner scan = new Scanner(System.in);
	Contato[] contatos;
	Contato contato;
	
	public Agenda() {
		contatos = new Contato[5];
	}
	
	public Contato[] adicionarContato() throws AgendaCheiaException{
		Scanner scan = new Scanner(System.in);
		boolean cheia = false;
		
		for(int i=0; i<contatos.length; i++) {
			if(contatos[i] != null) {				
				cheia = true;
			}			
		}
		
		if (cheia) {
			throw new AgendaCheiaException();
		}
		
		System.out.println("Digite o nome do contato");
		String nome = scan.nextLine();

		System.out.println("Digite o telefone com o DDD");
		String tel = scan.next();
		contatos[0].setNome(nome);
		contatos[contato.getIdentificador()-1].setTelefone(tel);
		contatos[contato.getIdentificador()-1].getIdentificador();
		return contatos;
		
	}

	public void consultarContatoNome(String nome) throws ContatoNaoExisteException{
		for(int i=0; i<contatos.length; i++) {
			if(contatos[i] != null) {
				if(contatos[i].getNome().equalsIgnoreCase(nome)) {
					
				}
			}
		}
		throw new ContatoNaoExisteException(nome);
	}
	
	public static void consultarContatoId(int id) {

	}
}
